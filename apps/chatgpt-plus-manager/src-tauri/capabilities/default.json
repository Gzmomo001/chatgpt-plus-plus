{
  "identifier": "default",
  "description": "Default permissions for the ChatGPT++ main window, including clipboard text writes and the settings file/folder picker.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "clipboard-manager:allow-write-text",
    "dialog:default"
  ]
}
